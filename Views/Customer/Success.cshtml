@{
    ViewData["Title"] = "Success";
}

<section class="bg-dark text-white py-5" style="min-height: 100vh; background: linear-gradient(to right, #141e30, #243b55);">
    <div class="container d-flex justify-content-center align-items-center flex-column text-center">
        <div class="p-5 rounded shadow-lg backdrop-blur-lg" style="
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px 0 rgba(0,0,0,0.37);
            max-width: 600px;
            width: 100%;
            color: #fff;">

            <i class="bi bi-check-circle-fill text-success fs-1 mb-3"></i>
            <h2 class="fw-bold text-info mb-3">Thank You!</h2>
            <p class="lead mb-4">Your information has been successfully submitted.</p>
            <a href="/" class="btn btn-outline-light">Back to Home</a>
        </div>
    </div>
</section>
